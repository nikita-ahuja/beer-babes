<div id="beer-comment">
    <h5><strong>Comments</strong></h5>
    <%= raw(beer.reviews.first.comments) %>
  </div>
  <div id="beer-note">
    <h5><strong>My Notes</strong></h5>
    <%= raw(beer.reviews.first.notes) %>
  </div>