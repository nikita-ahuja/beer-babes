<div class class="profile-container">
  <h2>Profile Page</h2>

  <!-- profile pic -->
Welcome, <%= current_user.username.upcase %>
  <!-- quiz results / preferences based on quiz -->

  <!-- form to add beer rating/review/notes -->


  <!-- beer suggestions based on personal reviews -->

  <!-- beer suggessions based on others who share the same preferences (from quiz) -->

  <!-- testing api -->
  <% if @beers %>
    <% @beers.each do |beer| %>
        <p>API ID: <%= beer.id %>, NAME: <%= beer.name %>, STYLE: <%= beer.style.name %> | DESCRIPTION: <%= beer.description %></p>
    <% end %>
  <% end %>

 <%= render 'shared/links' %>

</div>

 